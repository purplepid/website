import React from 'react';

import Style from './Nav.module.scss';

const Nav = () => {
    return (
        <React.Fragment>
            <div className={Style.navContent}>Navigation works!</div>
        </React.Fragment>
    );
};

export default Nav;
